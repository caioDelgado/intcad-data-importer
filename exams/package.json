{
  "name": "importer-intcad-exams",
  "version": "0.0.1",
  "description": "A importer of exams xlsx to intcad",
  "main": "index.js",
  "scripts": {
    "import-to-intcad": "node scripts/import-to-intcad.js",
    "convert-csv-to-json": "node scripts/convert-csv-to-json.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "standard --global after --global afterEach --global before --global beforeEach --global describe --global it"
  },
  "author": "Caio Delgado <caiodelgado.csd@gmail.com>",
  "license": "UNLICENSED",
  "dependencies": {
    "ascii-progress": "^1.0.5",
    "axios": "^0.18.0",
    "convert-csv-to-json": "^0.0.13",
    "crypto": "^1.0.1",
    "queue": "^4.4.2",
    "soap": "^0.24.0",
    "sugar-env": "^1.4.0"
  },
  "devDependencies": {
    "standard": "^10.0.3"
  }
}
